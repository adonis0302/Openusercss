---
platform: linux

inputs:
  - name: repo
  - name: npm-repo-cache

image_resource:
  type: docker-image
  source:
    repository: mhart/alpine-node

run:
  path: sh
  args:
    - repo/ci/scripts/build/prod.sh

params:
  NODE_ENV: production
