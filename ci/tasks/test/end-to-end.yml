---
platform: linux

inputs:
  - name: repo

image_resource:
  type: docker-image
  source: {repository: mhart/alpine-node}

run:
  path: sh
  args: [repo/ci/scripts/test/end-to-end.sh]

params:
  NODE_ENV: development
