checkNewVersion = false
defaultEntryPoints = ["http"]
InsecureSkipVerify = true

[entryPoints]
  [entryPoints.http]
  address = "[::]:80"

[retry]

[docker]
endpoint = "unix:///var/run/docker.sock"
domain = "openusercss.local"
watch = true
exposedbydefault = false
